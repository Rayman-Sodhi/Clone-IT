$(function() {
  // Custom Selects
  var $select = $('.select');
  if ($select.length) {
    $select.select2({
      theme: 'bootstrap',
      minimumResultsForSearch: Infinity,
    });
  }

  $('[data-toggle="popover"]').popover();
});
